import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourname.flappykrishna',
  appName: 'Flappy Krishna',
  webDir: 'src',
  server: {
    androidScheme: 'https'
  }
};

export default config;
